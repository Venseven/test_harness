import CrackStation
public struct test_harness {

    let decrypted = CrackStation.Crackstation().decrypt(shaHash: "<insert encrypted string")

    
    public private(set) var text = "Hello, World!"

    public init() {
    }
    // return dev
}
